to build the image
docker build --rm -t cw/dscc:latest .

to check the image
docker image ls | grep dscc

to run the image
docker run --rm -p 5000:5000 -p 5001:5001 -e ASPNETCORE_HTTP_PORT=https://+:5001 -e ASPNETCORE_URLS=http://+:5000 cw/dscc